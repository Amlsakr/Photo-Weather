<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeatherAmlSakrTask" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_blue</item>
        <item name="colorPrimaryVariant">@color/dark_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue</item>

        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MyCardViewStyle" parent="Theme.AppCompat.Light">
        <item name="cardCornerRadius">6dp</item>
        <item name="cardElevation">2dp</item>
        <!--        <item name="contentPaddingBottom">@dimen/dp_single</item>-->
        <!--        <item name="contentPaddingTop">@dimen/dp_single</item>-->
        <!--        <item name="contentPaddingLeft">@dimen/dp_single</item>-->
        <!--        <item name="contentPaddingRight">@dimen/dp_single</item>-->
        <item name="cardBackgroundColor">@color/backgroundLight</item>
    </style>
</resources>